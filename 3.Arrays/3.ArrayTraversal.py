# Array traversal = visiting each element of an array at least once

from array import *

array1=array("i",[1,4,12,13,10,0,15])

def traverseArray(array):
    for i in array:
        print(i)
traverseArray(array1)